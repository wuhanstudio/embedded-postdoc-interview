version: "3.3"

services:
  nginx:
    image: nginx:stable-alpine
    volumes:
      - ./docs:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/nginx.conf
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.embedded.rule=Host(`embedded.wuhanstudio.uk`)"
        - "traefik.http.routers.embedded.entrypoints=web"
        - "traefik.http.services.embedded.loadbalancer.server.port=80"
        # TLS
        - "traefik.http.routers.embeddeds.rule=Host(`embedded.wuhanstudio.uk`)"
        - "traefik.http.routers.embeddeds.entrypoints=websecure"
        - "traefik.http.routers.embeddeds.tls.certresolver=myhttpchallenge"
        # Redirect
        - "traefik.http.routers.embedded.middlewares=https_redirect"
        - "traefik.http.middlewares.https_redirect.redirectscheme.scheme=https"
    networks:
      - traefik-public
networks:
  traefik-public:
    external: true
